target "${dirname}" {
	require text cf io settings regexp xdiff
	headers src/{scm,status,gutter_diff}.h

	files resources/* "Resources"

	sources src/**/*.cc
	tests tests/t_*.cc
	frameworks Carbon Security
}
